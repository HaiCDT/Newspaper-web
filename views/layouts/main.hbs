<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar {
            background-color: #0056b3;
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: center; /* Căn giữa navbar */
        }
        .navbar-brand, .nav-link {
            color: white !important;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px 0;
            text-align: center;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px 0;
            text-align: center;
        }
        .content {
            padding: 20px;
        }
        .nav-item {
            display: flex;
            align-items: center;
        }
        .home-button {
            color: white;
            border: 1px solid white;
            border-radius: 5px;
            padding: 5px 10px;
            margin-left: 5px;
            transition: background-color 0.3s;
            display: inline-flex;
            align-items: center;
        }
        .home-button:hover {
            background-color: white;
            color: #0056b3;
        }
        #date-time {
            text-align: center;
            color: black;
            font-size: 1em;
            font-weight: bold;
            font-family: 'Courier New', monospace;
            padding: 0px 140px;
            padding-top: 4px;
        }

        /* Căn giữa nút CREATE trong navbar */
        .navbar-nav {
            width: 100%;
            display: flex;
            justify-content: center;
        }
        .navbar-nav .nav-item {
            flex: 0 0 auto; /* Đảm bảo không gian của item không bị co giãn */
        }
    </style>
</head>
<body>
    <!-- Tiêu đề chính -->
    <header class="header">
        <h1>Chào Mừng Bạn Đến Với Báo Online</h1>
        <p>Cập nhật tin tức mới nhất mỗi ngày!</p>
    </header>

    <!-- Navbar chuyên mục -->
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="navbar-nav">
            <a class="btn btn-outline-light" href="/articles/create" role="button">CREATE</a>
        </div>
    </nav>

    <!-- Nội dung chính -->
    <div class="content">
        <div class="container mt-4">
            {{{body}}}
        </div>
    </div>

    <!-- Chân trang -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 Báo Online. Tất cả quyền được bảo lưu.</p>
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
